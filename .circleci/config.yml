version: 2.1

jobs:
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - add_ssh_keys
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run:
          name: 'copy build'
          command: |
            ls -la
            scp  -r -o StrictHostKeyChecking=no ./* ubuntu@3.232.116.73:~

# Invoke jobs via workflows
workflows:
  say-hello-workflow:
    jobs:
      - deploy
