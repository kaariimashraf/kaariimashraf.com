version: 2.1

jobs:
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run:
          name: 'copy build'
          command: |
            scp  -r . ubuntu@server:~/new

# Invoke jobs via workflows
workflows:
  say-hello-workflow:
    jobs:
      - deploy
